# QR Chain Attendance System - Environment Configuration
# Feature: qr-chain-attendance
# Requirements: 18.1, 18.2, 18.3, 18.4, 18.5, 18.6, 18.7, 18.8

# ============================================================================
# REQUIRED CONFIGURATION
# ============================================================================

# Azure Storage Configuration (Requirement 18.1)
# The name of your Azure Storage Account
STORAGE_ACCOUNT_NAME=your-storage-account-name

# The URI endpoint for Azure Table Storage
# Format: https://<storage-account-name>.table.core.windows.net
STORAGE_ACCOUNT_URI=https://your-storage-account-name.table.core.windows.net

# Azure SignalR Service Configuration (Requirement 18.2)
# Connection string for Azure SignalR Service
# Format: Endpoint=https://<signalr-name>.service.signalr.net;AccessKey=<key>;Version=1.0;
SIGNALR_CONNECTION_STRING=Endpoint=https://your-signalr-service.service.signalr.net;AccessKey=your-access-key;Version=1.0;

# ============================================================================
# OPTIONAL CONFIGURATION - Token Settings
# ============================================================================

# Late Entry Token Rotation Period (Requirement 18.3)
# How often (in seconds) late entry QR codes rotate
# Default: 60 seconds
# LATE_ROTATION_SECONDS=60

# Early Leave Token Rotation Period (Requirement 18.4)
# How often (in seconds) early leave QR codes rotate
# Default: 60 seconds
# EARLY_LEAVE_ROTATION_SECONDS=60

# Chain Token Time-To-Live (Requirement 18.5)
# How long (in seconds) chain tokens remain valid
# Default: 20 seconds
# CHAIN_TOKEN_TTL_SECONDS=20

# Owner Transfer Flag (Requirement 18.6)
# Whether to transfer chain ownership (baton) after successful scan
# Default: true
# OWNER_TRANSFER=true

# ============================================================================
# OPTIONAL CONFIGURATION - Anti-Cheat Settings
# ============================================================================

# Wi-Fi SSID Allowlist (Requirement 18.7)
# Comma-separated list of allowed Wi-Fi network names
# When configured, students must be connected to one of these networks to scan
# Leave empty or comment out to disable Wi-Fi validation
# Example: WIFI_SSID_ALLOWLIST=ClassroomWiFi,SchoolNetwork,LabWiFi
# WIFI_SSID_ALLOWLIST=

# ============================================================================
# OPTIONAL CONFIGURATION - Azure OpenAI (AI Insights)
# ============================================================================

# Azure OpenAI Configuration (Requirement 18.8)
# When all three variables are configured, AI insights feature is enabled
# Leave empty or comment out to disable AI insights

# Azure OpenAI Endpoint
# Format: https://<resource-name>.openai.azure.com
# AOAI_ENDPOINT=

# Azure OpenAI API Key
# AOAI_KEY=

# Azure OpenAI Deployment Name
# The name of your GPT model deployment
# AOAI_DEPLOYMENT=

# ============================================================================
# NOTES
# ============================================================================

# 1. Required variables MUST be set for the system to function
# 2. Optional variables have sensible defaults if not specified
# 3. For production, use Azure Key Vault or Managed Identity instead of storing keys in .env
# 4. Never commit .env files with real credentials to version control
# 5. Copy this file to .env and fill in your actual values
# 6. Refer to DEPLOYMENT.md for detailed setup instructions
